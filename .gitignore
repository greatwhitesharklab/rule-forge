/*.png
step5-screenshots/

# === Java/Maven 构建产物 ===
target/
**/.flattened-pom.xml

# === Node/npm ===
node_modules/
dist/

# === V1 dev 运行(dev-lan.sh:日志/pid/环境变量)===
.dev-logs/
.dev-pids/
.env
.playwright-mcp/

# === 测试产物 ===
test-results/
**/test-results/
**/e2e/screenshots/

# === Python(cli / model-service)===
__pycache__/
*.pyc
.venv/
# model-service 运行数据(test 模型/metadata,运行生成)
model-service/data/

# neural-engine 训练产物(模型/记忆表快照/指标,运行生成)
experiments/neural-engine/artifacts/
# neural-engine 编排器训练产物(GRPO 模型权重 16GB+,不入 git)
experiments/neural-engine/eval/artifacts-orchestrator/

# === Java class(root 误生成兜底)===
*.class

# === ZCode / dev 运行数据 ===
.zcode/
data/


# UX 审计截图工作产物
.ux-audit/

# UX 走查脚本(一次性工具)
console-ui/scripts/ux-audit*.mjs

# neural-engine 验收实验运行产物(槽位库 work 文件,每次实验重建)
experiments/neural-engine/eval/artifacts*/work/
